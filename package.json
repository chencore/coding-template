{
  "name": "zhaojian",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "docker-compose up -d && npm --prefix backend run start:dev",
    "test": "npm --prefix backend run test && cd frontend && flutter test",
    "validate": "validation\\validate-template.cmd"
  }
}
